node_modules/
.next/
out/
*.tsbuildinfo
.DS_Store
# Generated, and its content depends on which command wrote it last: `next dev`
# points the route-types import at .next/dev, `next build` at .next, so a
# tracked copy is permanently dirty. `tsc` passes without it.
next-env.d.ts

# Written by `next dev`, and re-added if deleted.
AGENTS.md
CLAUDE.md

# Compiled from sw/sw.ts (see next.config.js).
public/sw.js
