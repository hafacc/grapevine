{
  "private": true,
  "name": "grapevine-shared",
  "version": "0.0.0",
  "type": "module",
  "description": "Pure TypeScript shared by the web app and the Edge Functions: no dependencies.",
  "main": "src/index.ts",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "test": "bun test",
    "fmt": "biome format --write",
    "lint": "tsc --noEmit && biome check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.10",
    "@types/bun": "^1.4.0",
    "typescript": "~7.0.2"
  }
}
